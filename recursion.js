// function element(numbers) {
    //     if (numbers.length==1){
    //        return numbers[0];
    //     }
    //     else if(numbers[0]>numbers[1]) {
    //        return element(numbers.slice(1));
    //     }else {
    //        return element([numbers[0]].concat(numbers.slice(2)));
    //     }
    // }
    // var numbers=[10,101,76,56,5,210,3,100];
    // console.log("The minimum element is="+element(numbers));
    
    // function element(numbers) {
    //    if (numbers.length==1){
    //       return numbers[0];
    //    }
    //    else if(numbers[0]<numbers[1]) {
    //       return element(numbers.slice(1));
    //    }else {
    //       return element([numbers[0]].concat(numbers.slice(2)));
    //    }
    // }
    // var numbers=[10,101,76,56,5,210,3,100];
    // console.log("The maximum element is="+element(numbers));